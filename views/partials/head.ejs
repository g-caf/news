<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Stay informed with the latest articles from your favorite publications">
    <title><%= title %> | NewsHub</title>
    <link rel="stylesheet" href="/assets/css/style.css">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* Atlantic-inspired Editorial Design */
        @import url('https://fonts.googleapis.com/css2?family=Lyon+Display:wght@400;700&family=AGaramondPro:wght@400;600&display=swap');
        
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body { 
            font-family: 'AGaramondPro', Georgia, 'Times New Roman', serif; 
            line-height: 1.7; 
            color: #1a1a1a; 
            background-color: #ffffff; 
            font-size: 18px;
        }
        
        .container { max-width: 1140px; margin: 0 auto; padding: 0 24px; }
        
        /* Header */
        .site-header { 
            background: #ffffff; 
            border-bottom: 1px solid #e0e0e0; 
            padding: 24px 0; 
            position: sticky; 
            top: 0; 
            z-index: 100;
        }
        
        .header-content { 
            display: flex; 
            align-items: center; 
            justify-content: space-between; 
        }
        
        .site-title a { 
            color: #c5282f; 
            text-decoration: none; 
            font-family: 'Lyon Display', Georgia, serif;
            font-size: 2.2rem; 
            font-weight: 700;
            letter-spacing: -0.02em;
        }
        
        .main-nav { display: flex; gap: 32px; }
        .nav-link { 
            color: #333; 
            text-decoration: none; 
            font-weight: 400; 
            font-size: 16px;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            transition: color 0.2s;
        }
        .nav-link:hover { color: #c5282f; }
        
        .search-form { display: flex; align-items: center; }
        .search-form input { 
            padding: 12px 16px; 
            border: 1px solid #d0d0d0; 
            font-family: inherit;
            font-size: 16px;
            width: 280px;
        }
        .search-form button { 
            padding: 12px 20px; 
            background: #c5282f; 
            color: white; 
            border: none; 
            cursor: pointer;
            font-family: inherit;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            font-size: 14px;
        }
        
        /* Main Content */
        .main-content { padding: 48px 0; }
        
        .page-header { 
            text-align: center; 
            margin-bottom: 48px; 
            padding-bottom: 24px;
            border-bottom: 1px solid #e0e0e0;
        }
        
        .page-header h1 { 
            font-family: 'Lyon Display', Georgia, serif;
            font-size: 3.5rem; 
            color: #1a1a1a; 
            font-weight: 400;
            margin-bottom: 16px;
            letter-spacing: -0.02em;
        }
        
        .page-header p {
            color: #666;
            font-size: 1.1rem;
            font-style: italic;
        }
        
        /* Publication Filters */
        .filter-section { 
            margin-bottom: 48px; 
            padding: 32px 0;
            border-bottom: 1px solid #e0e0e0;
        }
        
        .filter-section h3 {
            font-family: 'Lyon Display', Georgia, serif;
            font-size: 1.3rem;
            margin-bottom: 20px;
            color: #1a1a1a;
            font-weight: 400;
        }
        
        .publication-filters { 
            display: flex; 
            flex-wrap: wrap; 
            gap: 12px; 
        }
        
        .filter-btn { 
            padding: 8px 16px; 
            background: transparent; 
            color: #666; 
            text-decoration: none; 
            border: 1px solid #d0d0d0; 
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            transition: all 0.2s;
        }
        .filter-btn:hover { border-color: #c5282f; color: #c5282f; }
        .filter-btn.active { background: #c5282f; color: white; border-color: #c5282f; }
        
        /* Articles Layout */
        .articles-grid { 
            display: grid; 
            gap: 48px; 
            grid-template-columns: 1fr;
        }
        
        .article-card { 
            border-bottom: 1px solid #e0e0e0; 
            padding-bottom: 48px;
        }
        
        .article-meta { 
            margin-bottom: 16px; 
            display: flex; 
            align-items: center; 
            gap: 16px;
        }
        
        .publication-name { 
            color: #c5282f; 
            font-size: 14px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.1em;
        }
        
        .publish-date { 
            color: #999; 
            font-size: 14px;
            font-style: italic;
        }
        
        .article-title { 
            margin-bottom: 16px;
        }
        
        .article-title a { 
            color: #1a1a1a; 
            text-decoration: none; 
            font-family: 'Lyon Display', Georgia, serif;
            font-size: 2rem; 
            font-weight: 400;
            line-height: 1.3;
            letter-spacing: -0.01em;
            transition: color 0.2s;
        }
        .article-title a:hover { color: #c5282f; }
        
        .article-byline {
            color: #666;
            font-size: 16px;
            margin-bottom: 16px;
            font-style: italic;
        }
        
        .article-byline strong {
            color: #1a1a1a;
            font-weight: 600;
        }
        
        .article-description { 
            color: #444; 
            font-size: 1.1rem; 
            line-height: 1.6; 
            margin-bottom: 24px;
        }
        
        .article-actions {
            display: flex;
            gap: 20px;
            align-items: center;
        }
        
        .read-full { 
            background: #c5282f;
            color: white;
            text-decoration: none; 
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            font-weight: 600;
            padding: 12px 20px;
            transition: background 0.2s;
        }
        .read-full:hover { background: #a02128; }
        
        .read-original { 
            color: #c5282f; 
            text-decoration: none; 
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            font-weight: 600;
            border: 1px solid #c5282f;
            padding: 12px 20px;
            transition: all 0.2s;
        }
        .read-original:hover { 
            background: #c5282f; 
            color: white; 
        }
        
        /* Featured Article Layout */
        .article-card:first-child {
            grid-column: 1;
            padding: 32px;
            background: #fafafa;
            border: 1px solid #e0e0e0;
            border-bottom: 1px solid #e0e0e0;
        }
        
        .article-card:first-child .article-title a {
            font-size: 2.8rem;
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .container { padding: 0 16px; }
            .header-content { flex-direction: column; gap: 16px; }
            .site-title a { font-size: 1.8rem; }
            .page-header h1 { font-size: 2.5rem; }
            .article-title a { font-size: 1.6rem; }
            .article-card:first-child .article-title a { font-size: 2.2rem; }
            .search-form input { width: 100%; max-width: 280px; }
            .main-nav { gap: 20px; }
            .publication-filters { justify-content: center; }
        }
        
        /* Footer */
        .site-footer { 
            background: #1a1a1a; 
            color: #ccc; 
            text-align: center; 
            padding: 48px 0; 
            margin-top: 64px;
            font-size: 14px;
        }
    </style>
    <link rel="icon" href="/assets/img/favicon.ico">
</head>
